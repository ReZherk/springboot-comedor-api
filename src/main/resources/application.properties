# Oracle DB
spring.datasource.url=jdbc:oracle:thin:@localhost:1521:XE
spring.datasource.username=${DB_USERNAME:SYSTEM}
spring.datasource.password=${DB_PASSWORD:comeresbueno}
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver

# JPA
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.OracleDialect

# Puerto
server.port=8080

# JWT secreto (clave)
app.jwt.secret=${JWT_SECRET:MiClaveJWTSuperSegura2024!@#$%^&*()_+ABCDEFGHIJKLMNOPQRSTUVWXYZ}
app.jwt.expiration=86400000
app.jwt.refresh-expiration=604800000

# Logging (opcional - útil para debug)
logging.level.com.universidad.comedor=DEBUG
logging.level.org.springframework.security=DEBUG